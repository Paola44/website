<%= form_with(model: web, local: true) do |form| %>
  <% if web.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web.errors.count, "error") %> prohibited this web from being saved:</h2>

      <ul>
        <% web.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
